name: Daily AI Digest

on:
  schedule:
    - cron: '0 1 * * *'
  workflow_dispatch:

jobs:
  generate-digest:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.DAILY_DIGEST_TOKEN }}

      - name: Get last report date
        id: last-date
        run: |
          last_file=$(git log --oneline --format='' --name-only -1 | grep '\.md$' | grep -E '^[0-9]{4}-[0-9]{2}-[0-9]{2}' | head -1)
          if [ -n "$last_file" ]; then
            last_date=$(basename "$last_file" .md | sed 's/_AI.*//')
            echo "last_date=$last_date" >> $GITHUB_OUTPUT
          fi

      - name: Generate report
        env:
          LAST_DATE: ${{ steps.last-date.outputs.last_date }}
        run: node .github/workflows/generate.mjs

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Commit and push
        env:
          GH_TOKEN: ${{ secrets.DAILY_DIGEST_TOKEN }}
        run: |
          git remote set-url origin "https://x-access-token:${GH_TOKEN}@github.com/MiniDiva8/daily-ai-digest.git"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes, skipping"
          else
            DATE=$(date +%Y-%m-%d)
            git -c commit.gpgsign=false commit -m "docs: AI前沿速递 $DATE"
            git push
          fi

      - name: Show preview
        run: |
          FILE=$(ls -t *.md 2>/dev/null | head -1)
          if [ -n "$FILE" ]; then
            echo "=== 今日报告预览 ==="
            head -25 "$FILE"
          fi